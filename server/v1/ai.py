# pylint: disable=missing-module-docstring, missing-function-docstring, missing-class-docstring


async def ask(session_id: int, question: str) -> tuple[str, str]:
    # todo: fill in # pylint: disable=fixme
    return (
        f"dummy answer / session_id: {session_id}, question: {question}",
        "dummy advice",
    )
